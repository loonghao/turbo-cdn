name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  # 简单的发布设置
  release:
    uses: loonghao/rust-actions-toolkit/.github/workflows/reusable-release.yml@main
    with:
      rust-toolchain: stable